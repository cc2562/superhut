# 2026-08-22

## 课表周视图网格化 + 刷新入口移至「我的」+ 学期切换（测试用）

- **周视图网格化**（对齐 Flutter `lib/home/coursetable/view.dart`）：横轴 7 列星期（带日期+今天高亮）、纵轴节次 1-10、
  左侧节次轴（60rpx 宽，节次号）。课程块高度 = 节数 × 单节高（120rpx），按 startSection 定位、空白节插占位。
- domain-rules 新增 `courseColor`（字符串 hash → hue → HSL→hex，S/L=55/55，同名同色）和 `buildWeekSlots`
  （排序 + 占位 + 课程块，总高恒 10 节）。
- 「下一节课」卡片改 `mode === 'day'` 条件（只在日视图显示）。
- 刷新入口移到「我的」页 cell-group（「刷新课表」cell + profile.refreshTimetable），课表页 footer 只留「更新于」。
- **学期切换（测试用，后续删）**：后端 `refreshTimetable(token, semesterId)` 加可选 semesterId——指定学期时普通课
  `curriculum` 带 `xnxq01id`（复用 verifyHistoricalTimetable 已验证写法）；`fetchWeek` 加 `historical` 标志。
  前端 storage 按学期缓存（`Record<semesterId, TimetableCache>`），timetable 页 topbar 加学期 picker，onShow 拉
  semesters + 读该学期缓存（无则 refresh）。
- **坑**：fixture provider 的 `refreshTimetable(_token, _semesterId)` 两个未用参数触发
  `@typescript-eslint/no-unused-vars`（本项目 eslint 是 after-used 模式、**无 argsIgnorePattern '^_'**，`_` 前缀不忽略；
  有被用参数时前置 `_token` 不报，全未用则全报）。解法：方法体 `void _token; void _semesterId;`。
- CI 全绿：72 项测试（8 领域 + 20 客户端 + 44 服务端）。CHANGELOG 已更新。
- 真机待验证：网格单节高度可读性、历史学期 firstDay 兜底偏差。
