# SuperHUT Mini Changelog

## 2026-08-22 — 课表周视图网格化 + 刷新入口移至「我的」+ 学期切换（测试用）

状态：已完成

- 课表**周视图**改为「星期 × 节次」二维网格（对齐 Flutter）：横轴 7 列星期（带日期、今天高亮）、纵轴节次 1-10、左侧节次轴；课程块按起始节定位、高度 = 节数 × 单节高，按课程名 hash 配色（左彩条标识）。日视图保持按天列表。
- 「下一节课」卡片改为仅在日视图显示（周视图移除）。
- 「刷新课表」入口从课表页 footer 移至「我的」页（新增 cell），课表页 footer 仅保留「更新于 …」。
- 新增「选择学期查看课表」（**仅测试用**，新课表公布后删除）：课表页顶部下拉切换学期；后端 `refreshTimetable` 支持可选 `semesterId`（历史学期普通课带 `xnxq01id` 查询）；本地缓存按学期分 key（不同学期互不影响）。
- domain-rules 新增 `courseColor`（课程名 hash → HSL 配色）与 `buildWeekSlots`（节次块排布，含空白占位、总高恒 10 节）。
- `pnpm run ci` 通过：72 项测试（8 项领域规则、20 项客户端、44 项 BFF）及全部构建成功。
- 真机回归待验证：网格单节高度下课程块内容可读性、历史学期课表 `firstDay` 兜底偏差。

## 2026-08-21 — 空教室查询支持任意节次、座位数与空闲状态条

状态：已完成

- 空教室查询对齐 Flutter 版：新增「起始节 / 结束节」两个下拉（1~12 节），生成 4 位 nodeId 查询，不再写死第 1-2 节。
- 结果卡片新增座位数展示；点击教室展开 1~12 节空闲状态条（占用/空闲双色图例）。
- 后端 FreeRoomDto/FreeRoomSchema 扩展 `seatNumber`（座位数）与 `occupied`（占用节次区间，由 `zyjc` 解析：逗号分隔、每段去首字符，空串表全天空闲）。
- 服务端 nodeId 校验加强：起始/结束节 ∈ [1,12] 且起始 ≤ 结束，非法返回 VALIDATION_ERROR。
- 服务端新增 2 项契约/单测（freeRooms 解析 seatnumber/zyjc、isValidNodeId 边界）。
- `pnpm run ci` 通过：格式、Lint、类型、MySQL 迁移一致性、70 项测试（6 项领域规则、20 项客户端请求层、44 项 BFF）及全部构建成功。
- 真机回归待真实 `zyjc` 样本验证占用区间格式与状态条边界（如 `0112` 全占用）。

## 2026-08-21 — 教务 Token 过期检测与错误提示区分

状态：已完成

- 修复：教务 token 过期且未保存密码时，刷新课表/查成绩不再误报「学校服务不可用」，而是提示「教务登录已过期」并跳转登录页。
- 服务端新增 token 过期检测：业务查询遇上游 500 时二次调用 `/njwhd/noticeTab` 校验 token，过期则把 binding 标记为 expired 并抛 `AUTH_ACADEMIC_EXPIRED`；标记后后续查询直接返回过期，不再调学校。
- 错误文案区分：`ACADEMIC_UPSTREAM_UNAVAILABLE` 的 message 改为「学校服务器不可用」，明确是学校侧故障，与我方 `INTERNAL_ERROR` 的「服务暂时不可用」区分，避免用户误解为超级包菜服务器故障。
- 客户端 `toastRequestError` 对 `AUTH_ACADEMIC_EXPIRED` 提示「教务登录已过期，请重新登录」并跳转登录页；课表页刷新失败改用统一错误处理。
- `pnpm run ci` 通过：格式、Lint、类型、MySQL 迁移一致性、68 项测试（6 项领域规则、20 项客户端请求层、42 项 BFF）及全部构建成功。
- 真机回归待真实过期 token 验证业务接口响应码与跳转路径。

## 2026-08-21 — 成绩查询支持任意学期、全部学期与学分绩点汇总

状态：已完成

- 成绩查询对齐 Flutter 版：顶部新增学期下拉选择器（「全部学期」+ 各学期），支持查询任意学期；选「全部」时合并平铺展示所有学期成绩。
- 复用学校接口特性：`termGPA` 的 `semester` 参数留空即返回全部学期，无需新增上游接口。
- BFF scores 接口放开空 semesterId 拦截，并返回汇总字段（已修总学分 / 总学分绩点 / 平均学分绩点），前端顶部新增汇总卡展示。
- 学期下拉显示文案用 name 兜底 id（学校 semesterList 无独立 name 字段）。
- 服务端新增 3 项成绩 fixture 集成测试（单学期 / 全部学期 / 空结果与汇总字段）。
- 修复 CI 稳定性：server 新增 vitest.config.ts 固定 threads 池，规避 forks 池在多个测试文件各自 bootstrap NestJS/Fastify 应用时的 teardown 竞态（6 个测试文件时偶发非零退出码）。
- `pnpm run ci` 通过：格式、Lint、类型、MySQL 迁移一致性、64 项测试（6 项领域规则、18 项客户端请求层、40 项 BFF）及全部构建成功。
- 真机回归待真实成绩数据验证（汇总字段类型与空学期行为已在 fixture 层验证）。

## 2026-08-20 — 小程序版学生评教功能

状态：已完成（真机回归待评教季）

- 复刻 Flutter 版「学生评教」，新增批次选择 → 待评列表 → 答题三页流程，并从「服务」页新增入口。
- 支持三种评教方式：手动逐题选择提交、单课一键评教、一键评完当前批次所有未评课程（返回成功统计）。
- 自动评教算法忠实复刻 Flutter：第 1 题选低于 4.75 分的选项、其余题选最高分好评；无符合选项时兜底取最低/最高分，避免漏题导致提交失败。
- BFF 新增 6 个评教接口（批次/列表/题目/手动提交/单课自动/批量自动）；真实适配器的 post 扩展为支持 JSON body，继续沿用教务 Token 直连学校接口，不新增 Cookie/验证码依赖。
- 契约新增评教 Zod schema 与类型；服务端新增 10 项 fixture 集成与自动评教算法单测（fixture 数据不依赖真实评教内容）。
- `pnpm run ci` 通过：格式、Lint、类型、MySQL 迁移一致性、61 项测试（6 项领域规则、18 项客户端请求层、37 项 BFF）及全部构建成功。
- 真机回归待评教季：当前学校系统无待评课程，saveEvaluate 的 body 编码方式、是否额外需要 Cookie、批量评教耗时三项待真实待评内容出现后再验证。

## 2026-08-20 — 退出登录清除课表与教务信息展示

状态：已完成

- 退出登录语义调整：logout 现在同时清除本机课表缓存（此前仅清会话与凭据并保留课表缓存）。
  退出后小程序落在课表页的登录引导空态，“我的”页显示“未登录”且不再展示脱敏学号/姓名。
- 会话自愈收窄：401 AUTH_REQUIRED 的微信会话自动重建仅在“请求发出时本机持有 accessToken”
  （即会话过期场景）时触发；无 token（未登录/已退出）的请求直接抛 AUTH_REQUIRED，不再
  静默重建登录态，避免退出后任意查询页把用户“自动登录回来”。
- 微信会话改为按需显式建立：新增 ensureWechatSession()，登录页提交前调用；bootstrap 页不再
  自动创建会话，无 accessToken 时直接进入课表页登录引导空态。
- 成绩/考试/空教室页的 AUTH_REQUIRED 统一提示“请先登录教务账号”，其余错误沿用服务端信息。
- “我的”页新增 loggedIn 状态：未登录时隐藏“退出登录 / 解绑教务账号 / 注销 SuperHUT 账号”
  三个危险操作按钮，教务账号卡片按钮文案在“去登录 / 重新登录”间切换。
- `pnpm run ci` 通过：格式、Lint、类型、MySQL 迁移一致性、51 项测试（6 项领域规则、18 项
  客户端请求层、27 项 BFF 集成与安全）及全部构建成功。
- 与 8-19“退出后课表缓存保留”记录的差异：系用户要求退出后不再展示课程表与教务账号信息，
  同时避免会话自愈绕过退出状态；服务端 logout 单会话撤销与教务绑定保留语义不变。
- 待办：微信开发者工具模拟器/真机按“登录→退出→空态→再登录”回归；备案/类目/授权材料仍为
  人工项。

## 2026-08-20 — 客户端异常路径回归与会话自愈

状态：已完成

- 小程序端此前没有任何自动化测试；本轮为请求层新增 15 项异常路径测试，覆盖 401 自动
  刷新重试、刷新失败后的会话清理、教务过期自动重登、凭据失效删除、网络错误映射、
  错误码与请求 ID 透传及 INTERNAL_ERROR 兜底。
- 修复启动页缺陷：断网首开时微信会话创建失败未被捕获，用户会停留在“正在准备你的
  课表…”；现已纳入既有 catch，失败时跳转登录页。
- 新增会话自愈：refresh token 失效后自动通过云托管可信身份重建微信会话并重试一次原
  请求；隐私同意缺失或重建仍失败时保持 AUTH_REQUIRED 原错误，不产生请求循环。
- “我的”页解绑、退出、注销失败时补充 toast 反馈，不再静默无响应。
- `pnpm run ci` 通过：格式、Lint、类型、MySQL 迁移一致性、48 项测试（6 项领域规则、
  15 项客户端请求层、27 项 BFF 集成与安全）及全部构建成功。
- 本轮仅改动客户端 TypeScript 逻辑与测试，未触碰 WXML/WXSS 与 npm 依赖；微信开发者
  工具模拟器与真机异常路径回归仍待执行。

## 2026-08-20 — 微信云托管首次部署与连通验证

状态：已完成首次云端部署，小程序云托管连通测试成功

- 米饭已在生产云环境完成 `superhut-api` 首次部署，并确认云托管版本构建、启动及测试成功。
- 小程序继续通过 `wx.cloud.callContainer` 直连同环境服务，不依赖公网 API 域名、`wx.login`
  code、`code2Session` 或 AppSecret。
- 云端运行配置采用 Node.js 24 容器、`3000` 端口和 MySQL 8.0；Redis 已从首版运行时依赖中
  移除，限流、短期缓存与跨实例互斥由 MySQL 实现。
- Secret 与数据库连接信息仅在微信云托管环境变量中注入，本记录未保存密码、密钥、OpenID、
  完整学号、教务 Token 或查询正文。
- 本次结果证明“云托管部署 → 小程序调用”技术链路可用；正式提审仍需完成真机异常路径回归、
  小程序备案、主体类目、隐私指引及校方或服务方授权材料核验。

## 2026-08-19 — 微信云托管迁移

状态：MySQL-only 调整进行中；云服务与数据库已创建，环境变量和真实发布待完成

- 小程序请求层改为 `wx.cloud.callContainer`，微信登录改用云托管可信身份头，不再上传
  `wx.login` code 或使用 AppSecret。
- BFF 切换到 MySQL 8.0：持久化用户、教务绑定、加密快照和仅存摘要的轮换 session。
  云托管不内置 Redis，因此首轮改用 MySQL 保存限流计数和短期缓存，并使用 advisory lock
  实现跨实例登录/刷新互斥，避免额外购买腾讯云 Redis 和配置 VPC。
- 新增大小写敏感检查、受 advisory lock 保护的启动迁移、多阶段 Node.js 24 容器和云托管发布配置。
- `pnpm run ci` 通过：格式、Lint、类型、MySQL 迁移一致性、27 项测试及全部构建成功。
- 已在 `prod-d2gm96mrjfb4565b0` 创建 `superhut-api`；CLI 复核状态为正常，公网访问关闭，
  无默认公网域名或自定义域名。
- 本机未安装 Docker；控制台页面又禁止自动化操作，因此 Secret、远程
  镜像构建和开发者工具真实回归尚未完成，不将其记为发布成功。
- 安全扫描发现并移除了 Flutter 旧工具中的硬编码测试密码示例以及密码转换值、教务响应和
  Token 日志；请求、加密、登录与本地存储行为保持不变，定向 `flutter analyze` 通过。

本文件记录可验证的项目进度。禁止记录真实账号、密码、微信 code、AppSecret、教务 Token、完整个人资料或上游响应正文。

## 2026-08-18 — 工程骨架与 Fixture MVP

状态：进行中

### 已完成

- 建立与 Flutter 工程隔离的 `wechat-mini-program/` pnpm Monorepo。
- 建立原生 TypeScript + TDesign 小程序的启动、隐私、登录、课表、服务、成绩、考试、空教室和我的页面。
- 建立 NestJS/Fastify BFF、稳定响应包络、错误码、会话轮换、教务绑定和 Fixture/Real 双适配器。
- 建立 Drizzle PostgreSQL Schema 和初始迁移；六张业务表均无密码字段。
- 迁移严格日期、教学周、课程时间、课程排序、下一节课和稳定课程 ID 规则。
- 实现课表最后成功缓存、主动刷新原子替换、失败保留旧数据及更新时间展示。
- 实现微信异步加密 Storage 的可选本机密码保存、30 天到期和删除入口。
- 添加 systemd、Nginx、环境变量、CI 和阶段 0 核验资料。

### 验证证据

- `pnpm install` 成功并生成锁文件。
- `pnpm run ci` 已通过：格式、Lint、类型检查、测试与构建全部成功。
- 共 14 个测试通过：6 个课程领域测试与 8 个 BFF 集成/安全测试。
- Fixture 集成测试覆盖会话、教务绑定、课表快照、refresh token 轮换/防复用和登录限流。
- 构建产物 `server/dist/main.js` 已实际启动，`/health/live`、`/health/ready` 和 `/openapi.json` 冒烟通过。
- 敏感值审计未发现 Flutter 示例密码、学校密码转换密钥、私钥或已填写的微信 AppSecret。

### 阻塞项

- 尚未提供备案 HTTPS API 域名，不能完成微信真机公网请求验证。
- 开发 AppID 已配置；开发者工具生成的个人设置文件保持 Git 忽略。
- 真实教务测试账号由用户保管，本轮未获取凭据，因此未执行真实上游请求或保存 Fixture。
- PostgreSQL/Redis Repository 尚未接入运行时；真实模式 readiness 固定返回 503，当前版本不能部署为生产服务。
- 微信后台服务类目、备案、隐私指引和第三方高校查询授权仍需人工确认。

### 下一步

- 按 `docs/stage-0-checklist.md` 使用受控账号验证真实接口与 Token 生命周期。
- 将真实模式状态仓库接入 PostgreSQL/Redis 并验证重启后的会话恢复。
- 在微信开发者工具构建 npm，完成 Android/iOS 开发版真机回归。

## 2026-08-19 — 微信开发者工具 npm 构建修复

状态：已完成

### 已完成

- 将微信运行时 npm 入口放入小程序源码目录，构建产物仅生成到 `miniprogram/miniprogram_npm/`。
- 移除外层手动 npm 映射，避免开发者工具从错误目录查找组件。
- 将共享领域规则构建目标调整为 ES2019，以兼容微信开发者工具内置 npm 解析器。

### 验证证据

- 微信开发者工具 CLI `build-npm` 成功，零警告。
- 已确认 TDesign button、input、cell 组件和共享领域规则均存在于内层 `miniprogram_npm/`。
- 工作区类型检查通过；14 个领域、BFF 集成及安全测试全部通过。

## 2026-08-19 — Fixture 模拟器全链路调试

状态：已完成

### 已完成

- 接入微信开发者工具内置 `wechatide` 0.3.9；版本关系为 `equal`，登录态有效且无需 CLI Token。
- 修复成绩和考试页面把 `wx:elif`、`wx:for` 放在同一节点后导致的 WXML 条件分支编译错误。
- 注销客户端清理新增隐私同意键，确保注销后会话、课表、密码、视图模式和隐私同意均不残留。
- CI 忽略微信生成的内层 `miniprogram_npm`，避免对 TDesign 第三方构建产物执行格式和 Lint 检查。
- BFF 集成测试新增解绑级联、单会话退出和注销撤销全部会话覆盖。

### 验证证据

- 微信模拟器完成“隐私同意 → 微信会话 → 教务绑定 → 课表同步 → 再次启动读取缓存”流程。
- 成绩、考试、空教室、账号脱敏信息、周/日视图、日期移动、回到今天、主动刷新均通过运行时数据断言。
- 断开 Fixture BFF 后刷新仍保留原课表；恢复并重新建立会话后刷新成功。
- 退出后 access/refresh/密码键均不存在，课表缓存保留；解绑后课表与密码均删除；注销后相关客户端 Storage 键为空。
- 9 个页面的 WXML 和 WXSS 均通过微信开发者工具单文件编译。
- 微信 `build_npm` 成功且零警告；最终模拟器 console 无 WXML、语法、引用或未处理异常。
- `pnpm run ci` 全部通过；当前共 16 个测试通过（6 个领域测试、10 个 BFF 集成/安全测试）。

### 仍需验证

- 本轮仅使用脱敏 Fixture；真实教务账号、真实微信 `code2Session`、公网 HTTPS 域名和真机网络未纳入本次验证。
- PostgreSQL/Redis 运行时仓库尚未接入，Fixture BFF 重启后内存会话不会恢复。

## 2026-08-19 — 阶段 0 真实接入验证基线

状态：工具与契约已完成，等待受控账号交互验证

### 已完成

- 新增本地交互式 `stage0:verify`，AppSecret、学号和原始密码不接受命令行参数且不写仓库文件；输入后立即清屏。
- 教务密码转换规则自动从 Flutter 已有实现加载，不再要求用户输入协议转换密钥。
- 验证器通过微信开发者工具获取一次性登录 code，并按公开 BFF API 验证错误密码、教务绑定、课表、成绩、考试和空教室。
- 加固真实教务响应分类，区分凭据错误、Token 过期、非 JSON、结构变化、429、5xx 和网络超时。
- Token 检查不再把未知结构误判为过期；新登录 Token 必须立即验证后才建立绑定。
- 普通课和实验课的单周获取、转换已拆分，正式课表刷新仍只在完整成功后替换快照。
- 新增完全虚构的教务契约 Fixture；未保存任何真实账号、Token 或上游响应。

### 当前结论

- `pnpm run ci` 全部通过；当前共 27 个测试通过（6 个领域测试、21 个 BFF 集成、安全及真实适配器契约测试）。
- 微信开发者工具 `build_npm` 零警告，Fixture BFF 联通后刷新模拟器，最终 console 无小程序、WXML、语法、引用或未处理异常。
- 自动化与安全基线已具备，但尚未输入受控账号和运行时 Secret，因此不能给出技术 Go。
- PostgreSQL/Redis、备案 HTTPS 域名、真机公网链路、服务类目及授权材料仍不在本里程碑完成范围内。

## 2026-08-19 — 阶段 0 验证迁入小程序开发版

状态：技术验证通过，完整阶段 0 仍受合规与部署条件阻塞

### 已完成

- 开发版教务登录页新增“开始完整验证”，由小程序直接执行 `wx.login`、错误密码隔离、真实教务绑定、学期、课表、成绩、考试、教学楼和空教室查询。
- 验证页面仅显示记录数量、安全错误信息和请求 ID；不显示 AppSecret、密码、Token 或查询正文。
- 新增 `stage0:serve` 本机 BFF 启动入口；终端只临时接收服务端必需的 AppSecret，学号和原始密码改为仅在小程序页面输入。
- 教务密码转换规则继续自动复用 Flutter 实现，不复制转换密钥到小程序工程。
- 真实教务请求的 User-Agent 与现有 Flutter 行为基线对齐。
- 命令行验证器的虚构 Token 探针移至真实业务查询之后，学校接口对无效 Token 的临时错误不再阻止真实微信和教务链路先完成验证。

### 受控验证记录

- 一次旧版命令行验证在 `INVALID_TOKEN` 阶段收到 `ACADEMIC_UPSTREAM_UNAVAILABLE`，尚未进入微信会话或使用受控教务账号，因此不能据此判断账号登录能力。
- 开发版小程序实测已通过真实 `code2Session`、错误密码稳定分类且不建立绑定、真实教务登录、登录后 Token 检查和学期查询。
- 首次课表实测发现当前账号返回合法空课表时，旧逻辑因没有课程日期而无法计算学期首日；现已改为使用教学周接口的 `nowWeek` 严格推导空课表的学期锚点，并新增对应契约测试。
- 核对 Apifox 接口文档后确认普通课程接口已有 `xnxq01id` 与 `week` 参数；开发验证在当前课表为空时可读取上一个学期，但只返回安全数量摘要、不替换当前课表快照，且该路由仅由本机阶段 0 服务启用。
- 首次历史学期验证命中了无课程的短学期；现改为优先选择最近的常规 `-1`/`-2` 学期，并在找到首个有课周后停止请求，避免为了验证遍历整学期。
- 虚构 Token 实测返回学校特有的 HTTP 500 + JSON `code=401`；适配器只对这个精确组合映射登录过期，其他 5xx 仍映射上游不可用，避免掩盖学校故障。
- 最终开发版小程序验证通过：真实微信会话、错误密码隔离、教务登录、即时 Token 检查、学期、当前空课表、上学期真实课表、成绩真实空状态、考试真实空状态、教学楼和空教室均完成。
- 安全结果摘要：学期 15 个、当前课程 0 门、上学期首个有课周转换 5 门、成绩 0 条、考试 0 条、教学楼 28 个、一次空教室查询 40 间；仅记录数量和请求 ID，不记录正文。
- “本机记住密码”保持关闭，运行时 Storage 未出现教务凭据键；仓库扫描未发现完整学号、真实 Secret、`.env` 或真实响应，临时视觉核验截图已删除。
- 取证完成后已清理微信模拟器全部缓存、停止阶段 0 BFF、关闭运行时 Secret 进程并删除旧的安全摘要临时文件；验证状态不再留在本机运行环境中。

### 技术结论与剩余阻塞

- 技术结论：可行。真实受控账号已证明 BFF 适配器和开发版小程序链路可用。
- 完整阶段 0：仍受阻。备案 HTTPS API 域名、微信主体/服务类目材料、学校或服务方书面授权尚无完成证据。
- Token 长期寿命与自然限流表现继续低频观察，不主动压测学校接口；这不改变本轮即时登录与稳定错误分类已经通过的结论。

### 最终验证证据

- `pnpm run ci` 通过格式、Lint、类型、测试和全部 workspace 构建；共 30 项测试通过（6 项领域规则、24 项 BFF 集成、安全及真实适配器契约测试）。
- 微信开发者工具重新构建 npm 零警告，开发版真实验证页完成完整链路，模拟器无 error 或 exception。
- 独立低频虚构 Token 探针最终输出 `EXPIRED`；一般 5xx 的回归测试仍输出 `ACADEMIC_UPSTREAM_UNAVAILABLE`。
